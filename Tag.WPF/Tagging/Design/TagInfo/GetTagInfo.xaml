<UserControl x:Class="Tag.WPF.GetTagInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tag.WPF"
             xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             Width="200"
             Height="100"
             KeyDown="UserControl_KeyDown"
             >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <wpf:DialogHost Grid.RowSpan="2" Width="Auto" Height="Auto"/>

        <Button Grid.Row="0"
                Height="Auto"
                Margin="5"
                Content="MusicBrainz DB Search"
                Padding="0"
                Click="MusicBrainz_Search"
                />
        <Button Grid.Row="1"
                Margin="5"
                Height="Auto"
                Content="VGMDB.Net Search"
                Padding="0"
                Click="VGMDB_Search"
                />
    </Grid>
</UserControl>
