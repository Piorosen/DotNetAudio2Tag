<UserControl x:Class="Tag.WPF.Convert"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tag.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <!--<ItemsControl 
            Grid.Row="0"
            Background="Transparent">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border>

                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Margin="0,-130,0,0" Height="180" VerticalAlignment="Top">
                <Border Margin="5">
                    <TreeView
                    ItemsSource="{Binding ConvertExtension}"
                    RenderTransformOrigin="0.5,0.5"

                    >

                        <TreeView.Resources>
                            <HierarchicalDataTemplate
                        DataType="{x:Type local:ConvertModel}"
                        ItemsSource="{Binding Preset}">
                                <TextBlock
                                   Text="{Binding Title}" RenderTransformOrigin="0.5,0.5">
                                    <TextBlock.RenderTransform>
                                        <TransformGroup>
                                            <ScaleTransform/>
                                            <SkewTransform/>
                                            <RotateTransform Angle="180"/>
                                            <TranslateTransform/>
                                        </TransformGroup>
                                    </TextBlock.RenderTransform>
                                </TextBlock>
                            </HierarchicalDataTemplate>
                            <DataTemplate
                        DataType="{x:Type local:PresetModel}"
                        >
                                <TextBlock
                                       Text="{Binding Title}" RenderTransformOrigin="0.5,0.5">
                                    <TextBlock.RenderTransform>
                                        <TransformGroup>
                                            <ScaleTransform/>
                                            <SkewTransform/>
                                            <RotateTransform Angle="180"/>
                                            <TranslateTransform/>
                                        </TransformGroup>
                                    </TextBlock.RenderTransform>
                                </TextBlock>

                            </DataTemplate>
                        </TreeView.Resources>
                        <TreeView.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform/>
                                <SkewTransform/>
                                <RotateTransform Angle="180"/>
                                <TranslateTransform/>
                            </TransformGroup>
                        </TreeView.RenderTransform>
                    </TreeView>
                </Border>
            </Border>
        </Grid>
    </Grid>
</UserControl>
